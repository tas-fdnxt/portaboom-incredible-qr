# GATE — living Incredible QR (ICQR Magic Tree pattern)

Phone-width (390×844). `app.js?v=living1`.
DEST (unchanged): `https://www.trafficaccess.com.au/portaboom-product/portaboom-pb4000-series/`
ECC **H**, version 8, **49×49** modules generated by `qrcode@1.5.4` (packed in `qr-encode.js`).
SoT: twin-tidy5 livery as **module language** + optional Unit dock. Pattern from https://tree.icqr.com/ (3D that doubles as a scannable QR). No ICQR shop packs. No cherry tree. **Not** WordPress.

Default camera = orthographic top-down scan pose of the living matrix. Flatten-to-`qr.png` is gone. Print PNG is a secondary export and **must not** claim READY.

## SMOKE — live 3D scan

| Check | Result |
| --- | --- |
| Real H-matrix for DEST | PENDING — `destQr.size=49`, `ecc=H`, packed bits in `qr-encode.js` |
| Dark modules are 3D PORTABOOM vocab | PENDING — cabinet / boom stripe / head / LED / finder stainless |
| Default pose scannable | PENDING — decode `canvas.toDataURL` of default live WebGL (not `qr.png`) |
| Finder + timing readable | PENDING — classify `finder` / `timing` / `alignment` / `data` |
| Twin-tidy5 not lost | PENDING — Unit dock keeps planted twin, STOP Ø400, stripes, KINDCOL |
| Print PNG demoted | PENDING — button label Print PNG; status never READY on static art |

## STRESS — tilt / brightness

| Step | Result |
| --- | --- |
| Slight orbit tilt | PENDING — decode or document envelope |
| Life ON (tiny breathe + side LEDs) | PENDING — default pose still decodes |
| Full phone viewport (HUD on) | PENDING — HUD sits on white margin; document if decode fails |

## PRESSURE — not a textured quad

| Claim | Refute |
| --- | --- |
| It's just `qr.png` on a plane | PENDING — `scanPlanePresent=false`, no flatten-to-texture product |
| One textured quad | PENDING — `moduleMeshGroups === darkCount` (~1258 groups), `product=modular-geometry` |
| READY from print PNG | PENDING — `printClaimReady=false`; print is export only |

Do not merge. Chief reads this gate, then merges. NEVER SEND WordPress.
No Fabian Pages review link until Chief merges after GATE.
