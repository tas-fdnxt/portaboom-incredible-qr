# GATE — boom stripes + 400 mm STOP

Phone-width (390×844) against local Pages root. Named GLB. `app.js?v=twin-tidy4`.
SoT: hero photo of real PB4000 (attached) for tape + STOP scale. Live twin only for mount mechanics.
This is a first-format Pages bake tidy. **Not** living ICQR. **Not** WordPress. Do not merge — Chief reviews GATE then merges.

Three asks: hero chevrons · STOP Ø 400 mm · STOP movable along boom.

## SMOKE — pending browser verify

| Check | Result |
| --- | --- |
| Loads on phone-width | Pending |
| Boom stripes | Hero recipe: period 0.22 m, red duty 0.33 (white gaps ~2×), 45° `along − cross`, red `#c01421`, silver/white arm. Not 50/50 coral / band 0.12 |
| STOP size | Radius `0.20` → diameter 400 mm. `__iqr.snap.signDiameterM === 0.40` |
| STOP move | Dock slider + STOP in / STOP out. Default along 0.72 (not locked at tip). Range 0.32–0.90 |
| STOP upright | `tickSignUpright` counters pivot Z so legend stays readable as boom lowers |
| Round / Octagon | Kept |

## STRESS — pending browser verify

| Step | Result |
| --- | --- |
| Nudge in then out | `signAlong` steps by 0.04; slider + label update; face stays 400 mm |
| Slider drag | Position travels along boom; clamp stays with STOP |
| Boom lower | STOP stays upright; size unchanged |
| Flatten / restore | Stripe recipe + 400 mm + along persist |

## PRESSURE — pending (refutals)

| Claim | Refute |
| --- | --- |
| Stripes still 50/50 coral | Shader no longer `step(0.5, … / 0.12)`. Hero: duty 0.33, period 0.22, lean-away, no wash emissive |
| STOP still 450 mm | `SIGN_RADIUS = 0.20` overrides twin `0.225` |
| STOP locked at tip | Default `signAlong=0.72`; in/out + range move it |

## Ask set (this tidy)

1. Boom tape matches hero photo (thin true-red chevrons, wide white, silver arm)
2. STOP diameter 400 mm
3. STOP position adjustable; stays upright when boom lowers
4. Round / Octagon kept

Clean core + orbit + flatten-to-QR beat **kept** (not living ICQR).

Do not merge. Chief reads this gate, then merges. NEVER SEND WordPress.
