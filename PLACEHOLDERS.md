# Placeholder notes (Master Builder)

This Pages root is the living Incredible QR. Swap binaries in place. Do not nest under sunset-island-cruise-crew-chaos.

## `pb4000_named.glb`

Twin-tidy5 unit for **Unit dock**. `app.js` still loads `./pb4000_named.glb` in the background.

Keep the filename exactly `pb4000_named.glb` at repo root.

## `qr.png`

Legacy scan-plane art. The product is the **live 3D matrix**, not this file. Print export is generated from the DEST H-matrix in `qr-encode.js`.

Keep the filename if other links still point at it. Do not treat it as READY.

## Not a send

PREVIEW only. Do not email, post, or treat this as a live dispatch.
