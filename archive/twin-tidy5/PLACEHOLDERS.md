# Placeholder notes (Master Builder)

This Pages root is ready. Swap binaries in place. Do not nest under sunset-island-cruise-crew-chaos.

## `pb4000_named.glb`

Visible 3D subject. `app.js` loads `./pb4000_named.glb`, paints TAS navy `#1B2A4A` and orange `#EE7202`, then removes the hero stand-in.

If this file is still a stub, Master Builder pushes the real ~2.1MB named PB4000 GLB from M6 immediately after this preview lands.

Keep the filename exactly `pb4000_named.glb` at repo root.

## `qr.png`

Scan plane art. Tap flatten maps this texture onto the QR module grid at error-correction H.

Replace with the final scan-H PNG that encodes:

https://www.trafficaccess.com.au/portaboom-product/portaboom-pb4000-series/

Keep the filename exactly `qr.png` at repo root.

## Not a send

PREVIEW only. Do not email, post, or treat this as a live dispatch.
